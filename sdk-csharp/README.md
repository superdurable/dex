# Dex.Sdk (C# / .NET) — name placeholder

Reserves the [`Dex.Sdk`](https://www.nuget.org/packages/Dex.Sdk) package id on NuGet
(NuGet-idiomatic form of `dex-sdk`). C# namespace: `SuperDurable.Dex`.

This is **not** a functional SDK yet.
