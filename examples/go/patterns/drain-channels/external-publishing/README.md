# Draining Channel for External Publishing

## Endpoints

- `GET /patterns/drain-channels/external-publishing/start-or-publish?workflowId={workflowId}`

Call twice in a row to start, then publish another message.
