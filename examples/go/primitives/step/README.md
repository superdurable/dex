# Step primitive

Minimal two-Step Flow demonstrating `WaitFor`, `Execute`, and `GoTo`.

HTTP: `GET /primitives/step/start?workflowId=...&inputNum=1`

Also registers `RetryFlow`, a minimal Execute-retry demo.
