# @superdurable/dex-sdk (TypeScript) — name placeholder

Reserves [`@superdurable/dex-sdk`](https://www.npmjs.com/package/@superdurable/dex-sdk) on npm.

> Unscoped `dex-sdk` is **already taken** on npm, so this package uses the
> `@superdurable` scope (same idea as Java `io.superdurable`).

This is **not** a functional SDK yet.
