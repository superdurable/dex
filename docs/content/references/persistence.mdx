---
title: Persistence (Attributes)
---

# Persistence (Attributes)

**Attributes** are the durable variables of a Flow. An optional index config
makes an Attribute an Indexed Attribute for Flow search.

Use Attributes for state that must survive waits and be visible to RPCs/search. Prefer small values; large blobs use the blob store.
