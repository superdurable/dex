---
title: Channels
---

# Channels

Channels unify former signal / internal / interstate channels. Publish from clients, RPCs, or Steps; wait with Conditions (`for_one`, `for_n`, combinations).

Conditional completion when channels are empty is expressed with close decisions / force-complete-on-channels-empty options (see patterns: drain channels).
