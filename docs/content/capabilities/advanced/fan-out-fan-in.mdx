---
title: Fan-out + fan-in
---

# Fan-out + fan-in

## Why you need it

Many backends need to start concurrent work and join on all results or a quorum.

## Code examples

Choose an SDK (preference is sticky in this browser; default Python).

<SdkTabs>
<SdkSnippet lang="python">

```python
# Start parallel Steps then wait for N channel messages / completions
# See design pattern: parallel and scalable-parallel.
```

</SdkSnippet>
  <SdkSnippet lang="go">

```go
// Same idea in Go — see examples/go and sdk-go.
```

</SdkSnippet>
  <SdkSnippet lang="java">

```java
// Same idea in Java — see examples/java and sdk-java.
```

</SdkSnippet>
  <SdkSnippet lang="typescript">

```typescript
return goToMulti(
  StepMovement.of(workStep, item1),
  StepMovement.of(workStep, item2),
);
```

</SdkSnippet>
</SdkTabs>

Start multiple Steps (`GoToMulti` / multi movements) and wait on channels or step completion. A
first-success or quorum branch can also use [Step execution cancellation](./step-execution-cancellation)
to stop work that can no longer change the result.

## How Dex implements it

Dex schedules concurrent Step executions and joins via Channel conditions or WaitForStepCompletion.

## Traditional workarounds

Hand-rolled: thread pools + Futures, or map-reduce job systems with custom join tables.
