---
title: Draining Channel for External Publishing
---

import DrainingChannelForExternalPublishingDiagram from '@site/src/components/design-patterns/DrainingChannelForExternalPublishingDiagram';

# Draining Channel for External Publishing

Process externally published Channel messages, then close the short-lived Flow when its queue is empty.

## Problem and approach

Use this pattern when a Flow handles a burst of messages from outside the Worker, but has no durable work after that burst. An external publisher uses the Client **PublishToChannel** API or a Flow RPC to append a message. **ProcessMessage** waits for one message, processes it, then returns **ForceCompleteIfChannelsEmpty**.

The decision atomically checks the Channel while it records the next action. An empty Channel completes the Flow. A queued message takes the fallback movement back to **ProcessMessage**. After completion, the next request starts a new Flow execution with the same Flow ID before appending another message.

Only one Step may consume a Channel checked by **ForceCompleteIfChannelsEmpty**.

## Step graph

<DrainingChannelForExternalPublishingDiagram />

The graph shows Step definitions, not Step executions. **ProcessMessage** is the only Step definition; the fallback edge returns to it.

## Core implementation

<SdkTabs examples={{python: 'examples/python/dex_examples/patterns/drain-channels/external_publishing/draining_channel_flow.py', go: 'examples/go/patterns/drain-channels/external-publishing/workflow.go', java: 'examples/java/src/main/java/io/superdurable/dex/patterns/drainchannels/externalpublishing/DrainingChannelFlow.java', typescript: 'examples/typescript/src/patterns/drain-channels/external-publishing/draining-channel-flow.ts', rust: 'examples/rust/src/patterns/drain_channels/flow.rs'}}>
<SdkSnippet lang="python">

```python
def wait_for(self, context: Context, input: str) -> Wait:
    del context
    if input is None:
        return Wait.until(self.queue_channel.for_one())
    return Wait.skip_immediately()

return force_complete_if_channels_empty(
    None,
    StepMovement.of(self, None),
    self.queue_channel,
)
```

</SdkSnippet>
<SdkSnippet lang="go">

```go
func (processMessageStep) WaitFor(_ dex.Context, input string) (*dex.Wait, error) {
	if input == "" {
		return dex.Until(QueueChannel.ForOne()), nil
	}
	return dex.SkipWaitImmediately(), nil
}

return dex.ForceCompleteIfChannelsEmpty(
	nil,
	[]dex.ChannelDef{QueueChannel},
	dex.MovementOf(processMessageStep{}, ""),
), nil
```

</SdkSnippet>
<SdkSnippet lang="java">

```java
if (input == null) {
    return Wait.until(queueChannel.forOne());
}
return Wait.skipImmediately();

return StepDecision.forceCompleteIfChannelsEmpty(
        null,
        StepMovement.of(processMessage, null),
        queueChannel);
```

</SdkSnippet>
<SdkSnippet lang="typescript">

```typescript
if (input === undefined) {
  return Wait.until(queueChannel.forOne());
}
return Wait.skipImmediately();

return forceCompleteIfChannelsEmpty(
  null,
  StepMovement.of(this, undefined),
  queueChannel,
);
```

</SdkSnippet>
<SdkSnippet lang="rust">

```rust
pub const DRAIN_CHANNEL_PUBLISH: Rpc<String, ()> = Rpc::new("DrainChannelPublish");

Ok(StepDecision::force_complete_if_channels_empty(
    (),
    StepMovement::to(&DrainChannel, ()),
    [channel_queue().when_empty()],
))
```

</SdkSnippet>
</SdkTabs>

## Related

- [Channel](/primitives/channel)
- [Client publishing](/primitives/client/basic#publish-to-a-channel)
- [Examples on GitHub](https://github.com/superdurable/dex/tree/main/examples)
