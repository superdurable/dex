---
title: 排空外部 Channel 发布
---

import DrainingChannelForExternalPublishingDiagram from '@site/src/components/design-patterns/DrainingChannelForExternalPublishingDiagram';

# 排空外部 Channel 发布

处理外部发布到 Channel 的消息；队列为空时完成这条短生命周期的 Flow。

## 问题与做法

当一条 Flow 要处理 Worker 外部发来的一批消息，但这批消息处理完后没有其他 durable 工作时，使用这个模式。Client 可以通过 **PublishToChannel** 直接追加消息；这是单向操作，Client 不会收到 Flow 响应。活跃 Flow 也可以提供 **exampleRPC**，它向同一个 Channel 追加消息并返回响应。**ProcessMessage** 等待一条消息、处理它，然后返回 **ForceCompleteIfChannelsEmpty**。

这个 decision 会在记录下一步动作时原子地检查 Channel。Channel 为空时完成 Flow；仍有消息时，Dex 会走 fallback movement 回到 **ProcessMessage**。前一次 execution 完成后，下一个请求必须用相同的 Flow ID 启动新的 Flow execution，才能追加下一条消息。

被 **ForceCompleteIfChannelsEmpty** 检查的 Channel 只能由一个 Step 消费。

## Step 图

<DrainingChannelForExternalPublishingDiagram />

大边框表示 Flow definition。Channel 和 RPC 是外部接口，因此它们跨越这个边框。**queueChannel** 是单向的；**exampleRPC** 同时传递请求和响应。这张图表示 Step definition，不表示 Step execution。**ProcessMessage** 是唯一的 Step definition；fallback 边回到它。

## 核心实现

<SdkTabs examples={{python: 'examples/python/dex_examples/patterns/drain-channels/external_publishing/draining_channel_flow.py', go: 'examples/go/patterns/drain-channels/external-publishing/workflow.go', java: 'examples/java/src/main/java/io/superdurable/dex/patterns/drainchannels/externalpublishing/DrainingChannelFlow.java', typescript: 'examples/typescript/src/patterns/drain-channels/external-publishing/draining-channel-flow.ts', rust: 'examples/rust/src/patterns/drain_channels/flow.rs'}}>
<SdkSnippet lang="python">

```python
@rpc
def example_rpc(self, context: Context, input: str) -> RPCResult[str]:
    self.queue_channel.publish(context, input)
    return RPCResult(input)

def wait_for(self, context: Context, input: str) -> Wait:
    del context
    if input is None:
        return Wait.until(self.queue_channel.for_one())
    return Wait.skip_immediately()

return force_complete_if_channels_empty(
    None,
    StepMovement.of(self, None),
    self.queue_channel,
)
```

</SdkSnippet>
<SdkSnippet lang="go">

```go
func (*DrainingChannelFlow) ExampleRPC(
	ctx dex.Context,
	input string,
) (*dex.RPCResult[string], error) {
	if err := QueueChannel.Publish(ctx, input); err != nil {
		return nil, err
	}
	return &dex.RPCResult[string]{Output: input}, nil
}

func (processMessageStep) WaitFor(_ dex.Context, input string) (*dex.Wait, error) {
	if input == "" {
		return dex.Until(QueueChannel.ForOne()), nil
	}
	return dex.SkipWaitImmediately(), nil
}

return dex.ForceCompleteIfChannelsEmpty(
	nil,
	[]dex.ChannelDef{QueueChannel},
	dex.MovementOf(processMessageStep{}, ""),
), nil
```

</SdkSnippet>
<SdkSnippet lang="java">

```java
@RPC
public RPCResult<String> exampleRPC(final Context context, final String input) {
    queueChannel.publish(context, input);
    return RPCResult.of(input);
}

if (input == null) {
    return Wait.until(queueChannel.forOne());
}
return Wait.skipImmediately();

return StepDecision.forceCompleteIfChannelsEmpty(
        null,
        StepMovement.of(processMessage, null),
        queueChannel);
```

</SdkSnippet>
<SdkSnippet lang="typescript">

```typescript
@rpc({ inputCodec: stringCodec, outputCodec: stringCodec })
public exampleRPC(context: Context, input: string): RPCResult<string> {
  queueChannel.publish(context, input);
  return { output: input };
}

if (input === undefined) {
  return Wait.until(queueChannel.forOne());
}
return Wait.skipImmediately();

return forceCompleteIfChannelsEmpty(
  null,
  StepMovement.of(this, undefined),
  queueChannel,
);
```

</SdkSnippet>
<SdkSnippet lang="rust">

```rust
pub const EXAMPLE_RPC: Rpc<String, String> = Rpc::new("exampleRPC");

fn example_rpc(&self, context: &mut Context, input: String) -> HandlerResult<RpcResult<String>> {
    channel_queue().publish(context, input.clone())?;
    Ok(RpcResult::new(input))
}

Ok(StepDecision::force_complete_if_channels_empty(
    (),
    StepMovement::to(&DrainChannel, ()),
    [channel_queue().when_empty()],
))
```

</SdkSnippet>
</SdkTabs>

## 相关内容

- [Channel](/primitives/channel)
- [Client 发布](/primitives/client/basic#publish-to-a-channel)
- [GitHub 上的示例](https://github.com/superdurable/dex/tree/main/examples)
