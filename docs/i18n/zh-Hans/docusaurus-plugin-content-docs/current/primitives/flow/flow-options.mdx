---
title: Flow options
sidebar_label: Flow options
slug: /primitives/flow/flow-options
---

# Flow options

Dex 允许你自定义 Flow execution 的行为。

- **StartFlowOptions** 在客户端启动 Flow（或 SubFlow）时生效，启动后不可变。
  - 它们控制 timeout、ID reuse、cron schedule、初始 Attribute、request 幂等，以及该次 execution 的可选 config override。
- **FlowConfig** 提供运行时行为，启动后可变。
  - 包括 Step durability 默认值、Worker 路由、active-Step 搜索索引、continue-as-new 阈值与 Attribute Store projection target。

## StartFlowOptions {#start-flow-options}

| 字段 | 作用 |
|------|------|
| **Timeout** | Flow timeout。省略或设为零可禁用。 |
| **TimeoutPolicy** | Flow timeout 触发时的行为。 |
| **IDReusePolicy** | 启动时如何复用已有 Flow ID。 |
| **RequestID** | 重试同一次启动请求的幂等键。 |
| **AlreadyStartedOptions** | 如何处理已启动的 Flow。 |
| **CronSchedule** | 用于周期性启动的 cron 表达式。 |
| **RetryPolicy** | 单次 Flow execution 失败后的重试策略。 |
| **StartDelay** | start Step 运行前的延迟。 |
| **Attributes** | 初始 Attribute 值。 |
| **FlowConfig** | 每次 execution 的 Flow 配置。 |

<SdkTabs
  examples={{
    python: 'examples/python/dex_examples/primitives/flow/controller.py',
    go: 'examples/go/primitives/flow/controller.go',
    java: 'examples/java/src/main/java/io/superdurable/dex/primitives/flow/FlowController.java',
    typescript: 'examples/typescript/src/primitives/flow/controller.ts',
    rust: 'examples/rust/src/primitives/flow/controller.rs',
  }}>
  <SdkSnippet lang="python">

```python
def example_start_flow_options() -> StartFlowOptions:
    return StartFlowOptions(
        timeout=timedelta(minutes=30),
        timeout_policy=FlowTimeoutPolicy.HANDLER,
        start_delay=timedelta(minutes=5),
        id_reuse_policy=IdReusePolicy.DISALLOW,
        retry_policy=RetryPolicy(
            initial_interval=timedelta(minutes=1),
            backoff_coefficient=2,
            maximum_interval=timedelta(minutes=10),
            maximum_attempts=3,
        ),
        config_override=FlowConfig(step_durability=StepDurability.SYNC),
        ignore_already_started=True,
        request_id="start-order-123",
    ).with_attribute(status, "queued")
```

</SdkSnippet>
  <SdkSnippet lang="go">

```go
func exampleStartFlowOptions() (sdk.StartFlowOptions, error) {
	initialStatus, err := sdk.InitialAttribute(Status, "queued")
	if err != nil {
		return sdk.StartFlowOptions{}, err
	}
	timeout := 30 * time.Minute
	startDelay := 5 * time.Minute
	requestID := "start-order-123"
	stepDurability := sdk.StepDurabilitySync
	return sdk.StartFlowOptions{
		Timeout:       &timeout,
		TimeoutPolicy: sdk.TimeoutHandler,
		StartDelay:    &startDelay,
		IDReusePolicy: sdk.IDReuseDisallow,
		RetryPolicy: &sdk.FlowRetryPolicy{
			InitialInterval:    time.Minute,
			BackoffCoefficient: 2,
			MaximumInterval:    10 * time.Minute,
			MaximumAttempts:    3,
		},
		Attributes: []sdk.InitialAttributeDef{initialStatus},
		ConfigOverride: &sdk.FlowConfig{
			StepDurability: &stepDurability,
		},
		AlreadyStarted: &sdk.AlreadyStartedOptions{IgnoreError: true},
		RequestID:      &requestID,
	}, nil
}
```

</SdkSnippet>
  <SdkSnippet lang="java">

```java
private static StartFlowOptions exampleStartFlowOptions() {
    return StartFlowOptions.newBuilder()
            .timeout(Duration.ofMinutes(30))
            .timeoutPolicy(FlowTimeoutPolicy.HANDLER)
            .startDelay(Duration.ofMinutes(5))
            .idReusePolicy(IdReusePolicy.DISALLOW)
            .retryPolicy(
                    RetryPolicy.newBuilder()
                            .initialInterval(Duration.ofMinutes(1))
                            .backoffCoefficient(2)
                            .maximumInterval(Duration.ofMinutes(10))
                            .maximumAttempts(3)
                            .build())
            .addAttribute(ExampleFlow.status, "queued")
            .configOverride(
                    FlowConfig.newBuilder()
                            .stepDurability(StepDurability.SYNC)
                            .build())
            .ignoreAlreadyStarted(true)
            .requestId("start-order-123")
            .build();
}
```

</SdkSnippet>
  <SdkSnippet lang="typescript">

```typescript
export function exampleStartFlowOptions(): StartFlowOptions {
  return {
    timeoutMs: 30 * 60_000,
    timeoutPolicy: FlowTimeoutPolicy.HANDLER,
    startDelayMs: 5 * 60_000,
    idReusePolicy: IdReusePolicy.DISALLOW,
    retryPolicy: {
      initialIntervalMs: 60_000,
      backoffCoefficient: 2,
      maximumIntervalMs: 10 * 60_000,
      maximumAttempts: 3,
    },
    attributes: [InitialAttribute.of(status, "queued")],
    configOverride: {
      stepDurability: "sync",
    },
    ignoreAlreadyStarted: true,
    requestId: "start-order-123",
  };
}
```

</SdkSnippet>
  <SdkSnippet lang="rust">

```rust
pub fn example_start_flow_options() -> StartFlowOptions {
    StartFlowOptions::new()
        .timeout(Duration::from_secs(30 * 60))
        .timeout_policy(FlowTimeoutPolicy::Handler)
        .start_delay(Duration::from_secs(5 * 60))
        .id_reuse_policy(IdReusePolicy::Disallow)
        .retry_policy(
            RetryPolicy::new()
                .initial_interval(Duration::from_secs(60))
                .backoff_coefficient(2.0)
                .maximum_interval(Duration::from_secs(10 * 60))
                .maximum_attempts(3),
        )
        .initial_attribute(&status(), "queued".to_owned())
        .config_override(FlowConfig::new().step_durability(StepDurability::Sync))
        .ignore_already_started(true)
        .request_id("start-order-123")
}
```

</SdkSnippet>
</SdkTabs>

### Timeout 与 TimeoutPolicy {#timeout-and-timeout-policy}

**Timeout** 是整个 Flow 的一个 durable timer。只有 **Timeout** 为正数时才能设置 **TimeoutPolicy**。

- **FAIL** — 以 timeout error 失败 Flow。**RetryPolicy** 可以重试它。
- **CANCEL** — 取消 Flow，不会重试。
- **HANDLER** — 调用一次 Flow timeout handler。

提供正数 **Timeout** 时，Dex SDK 默认使用 **FAIL**。Flow 有 timeout handler 时，则使用 **HANDLER**。没有 handler 时使用 **HANDLER** 无效。一次 retry 会获得新的 timeout budget。

把 **TimeoutPolicy** 设为 **HANDLER**，timeout 触发时就会运行下面的代码。这个 handler 记录 Flow 结束的原因，再用应用自己的原因失败该 Flow。

<SdkTabs
  examples={{
    python: 'examples/python/dex_examples/primitives/flow/example_flow.py',
    go: 'examples/go/primitives/flow/workflow.go',
    java: 'examples/java/src/main/java/io/superdurable/dex/primitives/flow/ExampleFlow.java',
    typescript: 'examples/typescript/src/primitives/flow/example-flow.ts',
    rust: 'examples/rust/src/primitives/flow/flow.rs',
  }}>
  <SdkSnippet lang="python">

```python
def handle_timeout(self, context: Context) -> StepDecision:
    status.set(context, "timed out")
    return force_fail("processing deadline reached")
```

</SdkSnippet>
  <SdkSnippet lang="go">

```go
func (*ExampleFlow) HandleTimeout(ctx dex.Context) (*dex.StepDecision, error) {
	if err := Status.Set(ctx, "timed out"); err != nil {
		return nil, err
	}
	return dex.ForceFail("processing deadline reached"), nil
}
```

</SdkSnippet>
  <SdkSnippet lang="java">

```java
@Override
public StepDecision handleTimeout(final Context context) {
    status.set(context, "timed out");
    return StepDecision.forceFail("processing deadline reached");
}
```

</SdkSnippet>
  <SdkSnippet lang="typescript">

```typescript
public handleTimeout(context: Context): StepDecision {
  status.set(context, "timed out");
  return forceFail("processing deadline reached");
}
```

</SdkSnippet>
  <SdkSnippet lang="rust">

```rust
impl ExampleFlow {
    fn handle_timeout(&self, context: &mut Context) -> HandlerResult<StepDecision> {
        status().set(context, "timed out".to_string())?;
        Ok(StepDecision::force_fail("processing deadline reached"))
    }
}

impl Flow for ExampleFlow {
    fn timeout_handler(&self) -> Option<FlowTimeoutHandler<Self>> {
        Some(Self::handle_timeout)
    }
}
```

</SdkSnippet>
</SdkTabs>

### Flow ID、RequestID 与 AlreadyStartedOptions {#flow-id-request-id-and-already-started}

**IDReusePolicy** 决定这个 Flow ID 已存在时该怎么做。**RequestID** 表示这次调用是否在重试同一次启动请求。

| ID reuse policy | 结果 |
|-----------------|------|
| **IDReuseDefault** | 使用 Dex Server policy。没有 active run 时，StartFlow 允许复用。 |
| **IDReuseAllowIfPreviousFailed** | 只在上次 run 失败后复用该 ID。 |
| **IDReuseAllowIfNotRunning** | 任意 closed run 后都可复用，但 active run 期间不行。 |
| **IDReuseDisallow** | 永不复用该 ID。 |
| **IDReuseTerminateIfRunning** | 停止 active run，再启动 replacement。 |

调用方可能在丢失 StartFlow response 后重试时，应使用稳定的 **RequestID**。重试时发送相同的 Flow ID、start input、options 与 RequestID。启用 **AlreadyStartedOptions.IgnoreError** 后，Dex 只会在已有 Flow 使用相同 RequestID 启动时返回其 Run ID。不同 RequestID 仍会得到 already-started error。

### RetryPolicy 与 CronSchedule {#retry-policy-and-cron-schedule}

**RetryPolicy** 会在一次 Flow execution 失败后重试它。**MaximumAttempts** 包含首次 execution。**InitialInterval**、**BackoffCoefficient** 与 **MaximumInterval** 设置指数退避。**FAIL** timeout 可以使用这个 retry policy；**CANCEL** timeout 不可以。

**CronSchedule** 请求已配置的 backend 按计划启动新的 execution。它不是 Flow retry。retry 用来恢复一次失败的 execution；cron 创建之后的 execution。overlap 与漏掉运行怎么处理由 backend 决定。生产使用前请阅读 [cron schedule pattern](/design-patterns/cron)。

### StartDelay {#start-delay}

**StartDelay** 会立即接受 Flow start，然后等待 start Step 可以运行。它适合一次性延迟，例如新订单创建五分钟后再处理。它不会重复；周期性工作请使用 **CronSchedule**。

### Attributes {#attributes}

**Attributes** 会在 start Step 运行前写入 durable state。每个值必须匹配 Flow persistence schema 中的 Attribute 或 Attribute map。Attribute map 还需要 instance key。Dex 会拒绝未声明的名称、重复的 physical key，以及与已注册 map、index 或同步设置不一致的定义。

请使用 SDK 的 **InitialAttribute** 与 Attribute-map helper。start Step 读取这些值的方式和其它 Attribute 一样。

## FlowConfig {#flow-config}

在 **StartFlowOptions** 中使用 **ConfigOverride** 修改一个 Flow 的初始配置。它只修改提供的字段，不会整体替换 **FlowConfig**。

启动时，Dex 会把提供的字段覆盖到 Dex Server 默认值上。省略的字段保留默认值。

| 字段 | 行为 |
|------|------|
| **ActiveStepSearchMode** | 索引所有 active Step、只索引运行过 **WaitFor** 的 Step，或不索引 active Step。默认只索引带 **WaitFor** 的 Step。 |
| **ContinueAsNewThreshold** | 追踪到这个事件数后 continue as new。零会关闭它。 |
| **ContinueAsNewPageSizeBytes** | 限制 continue-as-new 带入的每一页 state。零使用 1 MiB。 |
| **StepDurability** | Step 写入的默认 durability。Step override 优先。除非显式覆盖，recovery 使用 synchronous durability。 |
| **WorkerTarget** | 后续 Step 与 RPC 调用的 WorkerService endpoint。可用它把后续工作移到新 Worker。 |
| **AttributeStoreName** | Dex Server Attribute Store，供 opt-in 异步投影的 Attribute 使用。空值会关闭后续投影。 |

### 用 Client 更新 FlowConfig {#update-flow-config-with-a-client}

Flow active 时可以调用 **UpdateFlowConfig**。只发送要修改的字段。修改会影响之后的 Flow decision，Dex 不会撤回已经派发的工作。
