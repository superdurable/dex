//! Placeholder crate reserving the `dex-sdk` name on crates.io.

/// Placeholder constant so the crate is not empty.
pub const SDK_NAME: &str = "dex-sdk";

/// Empty placeholder type for future Dex Rust SDK APIs.
pub struct DexClient;
