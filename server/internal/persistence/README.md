## Persistence 

Persistence layer is an abstraction for operating on database(SQL/NoSQL),
, so that engine layer doesn't need to understand internal details of database(e.g. SQL vs NoSQL, error handling) 